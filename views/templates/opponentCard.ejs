<div class="opponent-card col-3 p-0">
  <div class="image-container container d-flex justify-content-center align-items-center h-100 w-100" onclick="handleChampClick()" data-id="enemy<%= position + 1 %>">
    <img class="rounded-circle" src="<%= champ !== 'blank' ? imgURL : '/svg/plus.svg' %>" alt="">
  </div>
</div>
<script>
  const handleChampClick = () => {
    // set action to data-id
    const form = document.getElementById('champion-input-form');
    form.action = "/update?tar=" + event.target.parentNode.getAttribute('data-id');
    alert.log(imgUrl);
  }
</script>